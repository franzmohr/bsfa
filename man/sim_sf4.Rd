% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_sf4.R
\name{sim_sf4}
\alias{sim_sf4}
\title{Simulate data from a four-component stochastic frontier model}
\usage{
sim_sf4(
  n = 100,
  n_time = 8,
  beta = c(1, 0.5, 0.3),
  sigma_v = 0.2,
  sigma_mu = 0.3,
  par_eta = 8,
  par_u = 6,
  ineff = c("exponential", "halfnormal"),
  type = c("production", "cost")
)
}
\arguments{
\item{n}{number of units.}

\item{n_time}{number of periods per unit. Two is the minimum the model can
work with, and considerably more is needed before the split between the
unit effect and persistent inefficiency is informative.}

\item{beta}{frontier coefficients. The first element is the intercept, so a
vector of length \code{k} implies \code{k - 1} regressors named
\code{x1}, ..., \code{x(k-1)}.}

\item{sigma_v}{standard deviation of the symmetric error.}

\item{sigma_mu}{standard deviation of the unit effect.}

\item{par_eta}{parameter of the distribution of persistent inefficiency: the
rate for \code{ineff = "exponential"}, the scale for
\code{ineff = "halfnormal"}.}

\item{par_u}{the same for transient inefficiency.}

\item{ineff}{distribution of the two one-sided terms.}

\item{type}{either \code{"production"} or \code{"cost"}.}
}
\value{
A data frame with the dependent variable \code{y}, the regressors
  and a unit identifier \code{id}. The realised components are attached as
  the attributes \code{"mu"}, \code{"eta"} and \code{"u"}, and the three
  efficiency scores as \code{"persistent"}, \code{"transient"} and
  \code{"overall"}.
}
\description{
Generates artificial panel data from the model estimated by
\code{\link{create_sfmodel4_exp}} and \code{\link{create_sfmodel4_hn}}, for
use in examples, tests and prior predictive checks.
}
\examples{
set.seed(1234)
d <- sim_sf4(n = 50, n_time = 6, beta = c(1, 0.5, 0.3))
summary(attr(d, "persistent"))

}
\seealso{
\code{\link{sim_sf}} for the two-component models.
}
