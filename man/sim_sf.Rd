% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_sf.R
\name{sim_sf}
\alias{sim_sf}
\title{Simulate data from a stochastic frontier model}
\usage{
sim_sf(
  n,
  beta = c(1, 0.5, 0.3),
  sigma_v = 0.2,
  par_u = 4,
  ineff = c("exponential", "halfnormal"),
  type = c("production", "cost"),
  n_time = 1
)
}
\arguments{
\item{n}{number of units. With \code{n_time > 1} the returned data frame has
\code{n * n_time} rows and the inefficiency term is held fixed within a
unit, as in the time-invariant panel model.}

\item{beta}{frontier coefficients. The first element is the intercept, so a
vector of length \code{k} implies \code{k - 1} regressors named
\code{x1}, ..., \code{x(k-1)}.}

\item{sigma_v}{standard deviation of the symmetric error.}

\item{par_u}{parameter of the inefficiency distribution: the rate for
\code{ineff = "exponential"}, the scale for \code{ineff = "halfnormal"}.}

\item{ineff}{distribution of the inefficiency term.}

\item{type}{either \code{"production"} or \code{"cost"}.}

\item{n_time}{number of periods per unit.}
}
\value{
A data frame with the dependent variable \code{y}, the regressors,
  and a unit identifier \code{id}. The realised inefficiency terms and
  efficiency scores are attached as the attributes \code{"u"} and
  \code{"efficiency"}.
}
\description{
Generates artificial data from the model estimated by \code{\link{bsfa}},
for use in examples, tests and prior predictive checks.
}
\examples{
set.seed(1234)
d <- sim_sf(n = 50, beta = c(1, 0.5), sigma_v = 0.2, par_u = 4)
summary(attr(d, "efficiency"))

}
